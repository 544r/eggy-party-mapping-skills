---
name: eggy-party-args-blocks
description: 蛋仔派对取值积木完整列表
type: reference
originSessionId: fc80ad7e-341e-41a7-8b8d-90e2f860ddcf
---
# 蛋仔派对 - 取值积木完整列表

## 通用取值

| 积木名称 | 功能概述 | 参数 |
|---|---|---|
| 获取路径中的路点向量 | 取指定路径的第N个路点坐标 | 路径, 整数(索引) |
| 获取路径 | 取路径对象本身 | 路径 |
| 列表取值 | 从列表取出指定索引元素 | 列表, 整数 |
| 复制列表 | 复制新列表（不影响原列表） | 列表 |
| 列表中随机取值 | 随机取出N个元素（可重复） | 列表, 整数, 布尔 |
| 列表中随机取一个值 | 随机取出一个元素 | 列表 |
| 获取自定义属性 | 取对象的自定义字符串属性 | 对象, 字符串(属性名) |
| 获取自定义属性列表 | 取列表的自定义属性 | 同上 |
| 表格取值 | 取表格中指定行、列的值 | 表格, 行, 列 |
| 权重池中随机取一个值 | 按权重随机取出一个元素 | 权重池 |
| 权重池中随机取值 | 按权重随机取出N个元素 | 权重池, 整数, 布尔 |

---

## 游戏取值

| 积木名称 | 功能概述 | 参数 |
|---|---|---|
| 时刻是否自动流逝 | 判断时刻是否自动流逝 | — |
| 获取本局游戏游玩进度开启状态 | 取游玩进度是否开启 | — |
| 获取时间戳之间的秒差 | 计算两时间戳相差秒数 | 时间戳A, 时间戳B |
| 获取时间戳所在的时间 | 取时间戳对应的具体时间 | 时间戳, 时间单位字符串 |
| 转化时间戳为整数 | 把时间戳转为整数（秒） | 时间戳 |
| 获取当前时刻 | 取当前游戏时刻 | — |
| 获取时刻流逝速度 | 取时刻流逝速度值 | — |
| 获取互动按钮 | 取指定对象的按钮 | 对象, 整数(编号), 按钮类型 |
| 获取当前时间戳 | 取服务器当前时间戳 | — |
| 转化具体日期为时间戳 | 把日期转为时间戳 | 年、月、日、时、分、秒 |
| 转化整数为时间戳 | 把整数转为时间戳 | 整数 |
| 获取上次游玩时间戳 | 取玩家上次游玩时间戳 | 玩家 |

---

## 组件取值

| 积木名称 | 功能概述 | 参数 |
|---|---|---|
| 获取公告牌字体大小 | 取公告牌文本字体大小 | 组件 |
| 获取麻将/扑克牌牌数 | 取麻将或扑克牌数量 | 组件 |
| 获取组件当前皮肤ID | 取组件当前皮肤编号 | 组件 |
| 获取剩余定时销毁时间 | 取组件定时销毁剩余时间 | 组件 |
| 获取公告牌的文本内容 | 取公告牌显示的文本 | 组件 |
| 获取缩放倍率 | 取组件的缩放倍数 | 组件 |
| 获取组件所属玩家 | 取组件所属玩家 | 组件 |
| 获取绑定父组件 | 取组件的父组件 | 组件 |
| 获取角色绑定的所有子组件 | 取绑定到角色的所有子组件列表 | 角色 |
| 获取生物绑定的所有子组件 | 取绑定到生物的所有子组件列表 | 生物 |
| 获取射线检测内首个组件 | 取射线上第一个被碰撞的组件 | 起点坐标, 终点坐标 |
| 获取射线检测内多个组件 | 取射线上所有被碰撞的组件列表 | 起点, 终点, 数量 |
| 获取正在抓举的组件 | 取角色正在抓举的组件 | 角色 |
| 获取组件当前健康值 | 取组件当前生命值 | 组件 |
| 获取组件最大健康值 | 取组件最大生命值 | 组件 |
| 获取麻将/扑克牌牌面 | 取麻将或扑克牌面 | 组件 |
| 获取组件位置 | 取组件坐标位置 | 组件 |
| 获取组件缩放 | 取组件当前的缩放值 | 组件 |
| 获取组件旋转 | 取组件当前的旋转角 | 组件 |
| 获取组件指定朝向 | 取组件指定轴的朝向向量 | 组件, 方向轴类型 |
| 获取组件预设 | 取组件的预设类型 | 组件 |
| 获取组件线速度 | 取组件运动的线速度向量 | 组件 |
| 获取矩形区域内组件 | 取矩形范围内符合阵营关系的组件列表 | 中心点, 长, 高, 宽, 对象, 阵营关系 |
| 获取球形区域内组件 | 取球形范围内符合阵营关系的组件列表 | 中心点, 半径, 对象, 阵营关系 |
| 获取环形区域内组件 | 取环形范围内符合阵营关系的组件列表 | 中心点, 内径, 外径, 高, 对象, 阵营关系 |
| 获取扇形区域内组件 | 取扇形范围内符合阵营关系的组件列表 | 圆点, 朝向, 广角, 半径, 高, 对象, 阵营关系 |
| 获取圆柱区域内组件 | 取圆柱范围内符合阵营关系的组件列表 | 底面中心点, 半径, 高, 对象, 阵营关系 |
| 获取指定预设的组件列表 | 取场景中所有指定预设的组件列表 | 组件预设 |
| 获取组件指定染色区域的颜色 | 取组件染色区域的颜色 | 组件, 染色区域 |

---

## 角色/生物取值

| 积木名称 | 功能概述 | 参数 |
|---|---|---|
| 获取最大命数 | 取角色/生物的最大命数 | 角色/生物 |
| 获取当前命数 | 取角色/生物的当前命数 | 角色/生物 |
| 获取质量 | 取角色/生物的质量值 | 角色/生物 |
| 获取质量倍率（体重） | 取角色/生物的质量倍率 | 角色/生物 |
| 获取最大健康值 | 取角色/生物的最大生命值 | 角色/生物 |
| 获取当前健康值 | 取角色/生物的当前生命值 | 角色/生物 |
| 获取健康值恢复 | 取角色/生物每5秒恢复的生命值 | 角色/生物 |
| 获取移动属性 | 取角色/生物的移动属性 | 角色/生物, 移动参数类型 |
| 生命体获取击倒阈值 | 取生命体的击倒阈值 | 角色/生物 |
| 生命体获取空中击倒阈值 | 取生命体的空中击倒阈值 | 角色/生物 |
| 获取朝向 | 取角色/生物的朝向 | 角色/生物 |
| 获取线速度 | 取角色/生物的线速度向量 | 角色/生物 |
| 获取缩放 | 取角色/生物当前的缩放值 | 角色/生物 |
| 获取指定朝向 | 取角色/生物指定轴的朝向向量 | 角色/生物, 方向轴类型 |
| 获取角速度 | 取角色/生物的角速度 | 角色/生物 |
| 获取角色/生物所属玩家 | 取角色/生物所属玩家 | 角色/生物 |
| 获取旋转 | 取角色/生物当前的旋转角 | 角色/生物 |
| 获取当前等级 | 取角色/生物当前等级 | 角色/生物 |
| 获取技能拥有者 | 取技能的拥有者 | 技能 |
| 获取技能锁定角色/生物 | 取技能锁定的目标 | 技能 |
| 地图内所有角色/生物 | 取地图内全部角色/生物列表 | — |
| 获取玩家控制角色 | 取玩家控制的角色 | 玩家 |
| 获取技能指示器筛选的角色/生物列表 | 取符合技能指示器范围的角色/生物 | 技能, 高度 |
| 获取效果的来源 | 取效果的来源对象 | 效果 |
| 获取射线检测内首个角色/生物 | 取射线上第一个被碰撞的角色/生物 | 起点, 终点 |
| 获取射线检测内多个角色/生物 | 取射线上所有被碰撞的角色/生物列表 | 起点, 终点, 数量 |
| 获取关节的主体角色/生物 | 取关节的主体 | 关节 |
| 获取关节的目标角色/生物 | 取关节的目标 | 关节 |
| 获取正在抓举的角色/生物 | 取角色正在抓举的对象 | 角色/生物 |
| 获取角色/生物位置 | 取角色/生物的坐标 | 角色/生物 |
| 获取属性值（无视规则） | 取角色/生物的属性值 | 角色/生物, 属性索引 |
| 获取矩形区域内角色/生物 | 取矩形范围内符合阵营关系的角色/生物列表 | 中心点, 长, 高, 宽, 对象, 阵营关系 |
| 获取球形区域内角色/生物 | 取球形范围内符合阵营关系的角色/生物列表 | 中心点, 半径, 对象, 阵营关系 |
| 获取环形区域内角色/生物 | 取环形范围内符合阵营关系的角色/生物列表 | 中心点, 内径, 外径, 高, 对象, 阵营关系 |
| 获取扇形区域内角色/生物 | 取扇形范围内符合阵营关系的角色/生物列表 | 圆点, 朝向, 广角, 半径, 高, 对象, 阵营关系 |
| 获取圆柱区域内角色/生物 | 取圆柱范围内符合阵营关系的角色/生物列表 | 底面中心点, 半径, 高, 对象, 阵营关系 |

---

## 逻辑体取值

| 积木名称 | 功能概述 | 参数 |
|---|---|---|
| 获取剩余定时销毁时间 | 取逻辑体定时销毁剩余时间 | 逻辑体 |
| 获取传送门传送随机半径 | 取传送门的随机传送半径 | 逻辑体 |
| 获取光源亮度 | 取光源组件的亮度 | 逻辑体 |
| 获取缩放倍率 | 取逻辑体的缩放倍率 | 逻辑体 |
| 获取传送门传送坐标 | 取传送门的传送目标坐标 | 逻辑体 |
| 获取逻辑体所属玩家 | 取逻辑体所属玩家 | 逻辑体 |
| 获取角色绑定的所有子逻辑体 | 取绑定到角色的所有子逻辑体列表 | 角色 |
| 获取生物绑定的所有子逻辑体 | 取绑定到生物的所有子逻辑体列表 | 生物 |
| 获取逻辑体坐标 | 取逻辑体的坐标位置 | 逻辑体 |
| 获取当前缩放 | 取逻辑体当前的缩放值 | 逻辑体 |
| 获取逻辑体指定朝向 | 取逻辑体指定轴的朝向向量 | 逻辑体, 方向轴类型 |
| 获取逻辑体旋转 | 取逻辑体当前的旋转角 | 逻辑体 |
| 获取逻辑体预设 | 取逻辑体的预设类型 | 逻辑体 |
| 获取绑定组件的所有子逻辑体 | 取绑定到组件的所有子逻辑体列表 | 组件 |
| 获取指定预设的逻辑体列表 | 取场景中所有指定预设的逻辑体 | 逻辑体预设 |

---

## 触发区域取值

| 积木名称 | 功能概述 | 参数 |
|---|---|---|
| 获取剩余定时销毁时间 | 取触发区域定时销毁剩余时间 | 触发区域 |
| 获取当前缩放 | 取触发区域当前的缩放值 | 触发区域 |
| 获取触发区域指定朝向 | 取触发区域指定轴的朝向向量 | 触发区域, 方向轴类型 |
| 获取缩放倍率 | 取触发区域的缩放倍率 | 触发区域 |
| 获取存档区域复活朝向 | 取存档区域的复活朝向 | 逻辑体 |
| 获取触发区域坐标 | 取触发区域的位置坐标 | 触发区域 |
| 获取存档区域复活坐标 | 取存档区域的复活坐标 | 逻辑体 |
| 获取触发区域的所属玩家 | 取触发区域所属玩家 | 触发区域 |
| 获取绑定组件的所有子触发区域 | 取绑定到组件的所有子触发区域列表 | 组件 |
| 获取角色绑定的所有子触发区域 | 取绑定到角色的所有子触发区域列表 | 角色 |
| 获取生物绑定的所有子触发区域 | 取绑定到生物的所有子触发区域列表 | 生物 |
| 获取射线检测内首个触发区域 | 取射线上第一个被碰撞的触发区域 | 起点, 终点 |
| 获取射线检测内多个触发区域 | 取射线上所有被碰撞的触发区域列表 | 起点, 终点, 数量 |
| 获取触发区域旋转 | 取触发区域的旋转角度 | 触发区域 |
| 获取指定预设的触发区域列表 | 取场景中所有指定预设的触发区域 | 触发区域预设 |

---

## 数学取值

| 积木名称 | 功能概述 | 参数 |
|---|---|---|
| 整数运算(+-×÷) | 整数基本运算 | 整数A, 运算符, 整数B |
| 实数运算(+-×÷) | 实数基本运算 | 定点数A, 运算符, 定点数B |
| 向量运算(+-×÷) | 向量加减乘除 | 运算符, 向量A, 向量B |
| 取绝对值（整数） | 取整数绝对值 | 整数 |
| 阶乘 | 计算整数阶乘 | 整数 |
| 排列 | 计算排列数P(n,m) | n, m |
| 组合 | 计算组合数C(n,m) | n, m |
| 最大公约数 | 两整数最大公约数 | 整数A, 整数B |
| 最小公倍数 | 两整数最小公倍数 | 整数A, 整数B |
| 正弦 | sin(角度制) | 定点数 |
| 余弦 | cos(角度制) | 定点数 |
| 正切 | tan(角度制) | 定点数 |
| 反正弦 | asin(角度制) | 定点数 |
| 反余弦 | acos(角度制) | 定点数 |
| 反正切 | atan(角度制) | 定点数 |
| 反正切2 | atan2(角度制) | x, y |
| 取绝对值（定点数） | 取实数绝对值 | 定点数 |
| 开方 | 开n次方 | 被开方数, 指数 |
| 对数 | 以底数为底的对数 | 真数, 底数 |

---

## 定点数取值

| 积木名称 | 功能概述 | 参数 |
|---|---|---|
| 转整数（向下取整） | 向下取整转整数 | 定点数 |
| 转整数（自选方式） | 按指定取整方式转整数 | 定点数, 取整方式 |
| 转整数（四舍五入） | 四舍五入转整数 | 定点数 |
| 获取血条缩放宽度 | 取角色血条宽度缩放值 | 角色/生物 |
| 获取血条缩放高度 | 取角色血条高度缩放值 | 角色/生物 |
| 向量的点乘 | 两向量点积 | 向量A, 向量B |
| 获取实数线性插值 | 在两数之间按比例插值 | 起点, 终点, 比例 |
| 获取实数线性插值列表 | 在两数之间按间隔生成列表 | 起点, 终点, 间隔 |
| 获取最终攻击间隔 | 根据攻速计算最终攻击间隔 | 角色/生物 |
| 获取物体之间距离 | 计算两对象间的距离 | 对象A, 对象B |
| 随机实数 | 在指定区间产生随机数 | 最小值, 最大值 |
| 格式化实数 | 按进位规则保留小数位 | 实数, 进位方式, 小数位数 |
| 取较大值 | 返回两数中的较大者 | 数A, 数B |
| 取较小值 | 返回两数中的较小者 | 数A, 数B |
